# Configuração do Servidor
PORT=3333
NODE_ENV=development

# Banco de Dados (PostgreSQL)
# Ajuste o USER e PASSWORD conforme o seu banco local
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=1209
DB_NAME=carona_api
DB_NAME_TEST=carona_api_test

# Autenticação (JWT)
JWT_SECRET=segredo-super-secreto-do-borala
JWT_EXPIRES_IN=1d